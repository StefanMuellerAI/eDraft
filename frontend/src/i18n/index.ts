import { de, type Messages } from './de'

/**
 * Aktive Sprache. Später: Umschaltung z. B. über Browser-Locale oder
 * Nutzereinstellung; Komponenten greifen ausschließlich über `t` zu.
 */
export const t: Messages = de
